const initialState = {
  user: 'Bom dia'
};

const reducer = (state = initialState, action) => {
 
  if (action.type === 'ON_SUBMIT') {
    return {
      
    }
  }
  return state;
};

export default reducer;
